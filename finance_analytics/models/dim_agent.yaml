version: 2
name: dim_agent
layer: DWS
table_type: fact
config:
  materialized: full
entities:
- code: AGENT
  type: primary
  name: 代理商
  key_columns:
  - agent_key
business_area: CLNT
atomic_metrics:
- total_interactions
derived_metrics:
- avg_interaction_duration
- avg_satisfaction_rating
- avg_sentiment_score
calculated_metrics:
- resolution_rate
- escalation_rate
grain:
  entities:
  - AGENT

version: 2
name: dim_store_metric_snapshot
layer: ADS
table_type: fact
description: DIM anti-pattern fixture with metric group metadata
config:
  materialized: incremental
execution:
  slice:
    param: etl_date
    column: snapshot_date
    period: D
atomic_metrics:
- store_order_count
entities:
- code: STORE
  type: foreign
  name: 门店
  key_columns:
  - store_id

# BENCHMARK FIXTURE ONLY.
# This warehouse copy is used as YAML metadata gold labels for the
# table_inspector_layer cold-start benchmark. Do not run task_run,
# reinit_project, import_lineage, or refactor execution commands against it:
# SQL/DDL/task files are copied byte-for-byte from warehouses/shop and still
# reference the source Doris databases.
name: shop_layering_fix
fixture:
  source_project: shop
  purpose: table_inspector_layer_benchmark_gold_labels
  execution: disabled
  warning: Do not execute this project; metadata fixture only.
catalog: internal
database: shop_dm
qa_database: shop_dm_qa
lineage_database: shop_lineage
naming_config: naming_config.yaml
verification:
  default_refresh_parameter: etl_date
  week_start: MON
  row_compare:
    exclude_columns:
    - etl_time
default_dialect: doris
ods_source_catalog_dialects:
  internal: doris

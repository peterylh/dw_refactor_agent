version: 2
name: dwd_order_summary_bad
layer: ADS
table_type: fact
description: DWD anti-pattern fixture with aggregation
config:
  materialized: incremental
atomic_metrics:
- item_count
entities:
- code: ORDER
  type: foreign
  name: 订单
  key_columns:
  - order_id
business_process: ORDER_TRANSACTION
derived_metrics:
- name: total_amount
  base_metric: subtotal
  base_metric_table: ods_order_item
  time_period: D
  expression: SUM(oi.subtotal)
  aggregation: SUM

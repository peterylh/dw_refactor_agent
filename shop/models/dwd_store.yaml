version: 2
name: dwd_store
layer: DIM
table_type: dimension
description: 门店每日快照
config:
  materialized: snapshot
entities:
- code: STORE
  type: primary
  name: 门店
  key_columns:
  - store_id
semantic_subject: STORE
